import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Smart GST Calculator | Inclusive & Exclusive GST",
  description:
    "A modern GST calculator for India. Quickly compute GST for inclusive and exclusive amounts at 5%, 12%, 18%, and 28% rates.",
  keywords: [
    "GST calculator",
    "India GST",
    "inclusive GST",
    "exclusive GST",
    "tax calculator",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
